###################################################################/
# Descrption: sw.data
#
###################################################################/
sw.data = function( dir, external = FALSE )
{
  data = data.table( x = 1, pValue = 0.1 )
  return( data )
}
