# exposed functions
export( mi.univariate.subset  )
export( mi.univariate )
export( plot.qq )
export( ABO.univariate )
export( ABO.lk.surfs )
export( ABO.lk.pars )
export( calc.lBF )
export( marginal.posterior.profile )
export( draw_tree )
export( draw_tree_univariate )
export( trim_tree )
export( grs.draw_tree )
export( grs.get.posterior.node_1d )
export( grs.calc.llk.tree )
export( grs.calc.lBF )
export( grs.marginal.posterior )
export( MI_GRS.lk.surfs )
export( grs.plot.lk.surface )


# packages used
import( data.table )
import( plotly )
import( Rgraphviz )
import( graph )
import( BiocGenerics )
import( parallel )
import( magrittr )
